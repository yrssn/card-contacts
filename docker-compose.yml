services:
  app:
    build: .
    image: card-contacts:latest
    container_name: card-contacts
    ports:
      - "127.0.0.1:9282:8000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
